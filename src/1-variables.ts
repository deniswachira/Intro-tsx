export const variables = () => {
    let a: number = 10;
    let name: string = "Denis";
    let gender: boolean = true;


    console.log("--TypeScript Variables--");
    console.log(`typeof a: ${typeof a} && value of a: ${a}`);
    console.log(`typeof b: ${typeof name} && value of b: ${name}`);
    console.log(`typeof c: ${typeof gender} && valueof sex:${gender}`)
};